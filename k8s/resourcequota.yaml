apiVersion: v1
kind: ResourceQuota
metadata:
  name: graph-kb-quota
  namespace: graph-kb
spec:
  hard:
    requests.storage: "35Gi"
    pods: "5"
    services: "5"
    secrets: "10"
    configmaps: "10"
